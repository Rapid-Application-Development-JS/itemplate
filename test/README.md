You should have global installed `mocha`:

```bash
node install mocha -g
```
```javascript
            require('jsonfile').writeFile('./test/data/test1.json', jsondata, function (err) {
                console.error(err)
            })
```
